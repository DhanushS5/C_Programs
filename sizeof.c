 #include <stdio.h>
int main()
{
    int a;
    float b;
    char c;
    double d;
    printf("the size of a(integer) is %zu\n",sizeof(a));
    printf("the size of b(float) is %zu\n",sizeof(b));
    printf("the size of c(char) is %zu\n",sizeof(c));
    printf("the size of d(double) is %zu\n",sizeof(d));
    return 0;
}
